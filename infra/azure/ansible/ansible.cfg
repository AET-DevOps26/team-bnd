[defaults]
inject_facts_as_vars = False
# Fresh VMs have no known_hosts entry yet, so accept the key instead of prompting.
host_key_checking = False
# The deprecation notice comes from inside an ansible-core module we do not control.
deprecation_warnings = False
