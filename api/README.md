# API
We use [Swagger Editor](https://editor.swagger.io/) to define our API. The respective client implementations can then be generated via tools as described in the "Best Practices Microservices" section on Artemis.

Changes to the API should lead to an updated version number, such that clients don't break on update.