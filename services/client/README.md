# Alexandria Web Client

## Installation

Build locally:

```bash
docker build -t team-bnd-client .
docker run -p 8080:80 team-bnd-client
```

Or for development (requires `node`/`npm`):

```bash
npm install
npm run dev
```
